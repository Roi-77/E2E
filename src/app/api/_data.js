// src/app/api/_data.js
// Simple in-memory storage for demo. Replace with DB in production.

export const users = {
  // username: { password, publicKey: base64_spki }
};

export const messages = [
  // { sender, recipient, ciphertext, iv (base64), encryptedAESKey (base64 | optional) }
];
